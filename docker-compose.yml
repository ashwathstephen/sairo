version: "3.8"
services:
  sairo:
    build: .
    ports:
      - "8000:8000"
    environment:
      S3_ENDPOINT: ${S3_ENDPOINT:?Set S3_ENDPOINT in .env}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:?Set S3_ACCESS_KEY in .env}
      S3_SECRET_KEY: ${S3_SECRET_KEY:?Set S3_SECRET_KEY in .env}
      S3_REGION: ${S3_REGION:-us-east-1}
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASS: ${ADMIN_PASS:?Set ADMIN_PASS in .env}
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env (use a random 32+ char string)}
      SECURE_COOKIE: ${SECURE_COOKIE:-true}
      RECRAWL_INTERVAL: ${RECRAWL_INTERVAL:-120}
    volumes:
      - sairo-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz')"]
      interval: 30s
      timeout: 5s
      retries: 3

  # Optional: AI-powered storage intelligence (MCP server)
  # Uncomment to enable AI assistant access via Claude Desktop, Cursor, etc.
  # sairo-mcp:
  #   build: ./mcp
  #   ports:
  #     - "8100:8100"
  #   environment:
  #     SAIRO_API_URL: http://sairo:8000
  #     SAIRO_API_TOKEN: ${MCP_SERVICE_TOKEN:-}
  #     DB_DIR: /data
  #     MCP_BIND_HOST: "0.0.0.0"
  #     MCP_PORT: "8100"
  #   volumes:
  #     - sairo-data:/data:ro
  #   depends_on:
  #     sairo:
  #       condition: service_healthy
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8100/healthz')"]
  #     interval: 30s
  #     timeout: 5s
  #     retries: 3

volumes:
  sairo-data:
